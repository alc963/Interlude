version: "3.8"
services:
  backend:
    build: ./backend
    restart: always
    ports:
      - "127.0.0.1:8082:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
  frontend:
    build: ./frontend
    restart: always
    ports:
      - "127.0.0.1:8081:80"
    depends_on:
      - backend